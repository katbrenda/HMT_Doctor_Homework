



const List = ({data,renderData})=>{
    const renderList =()=>{
        if(!data){
            throw 'data prop required'

        }
        if(!renderData){
            JSON.stringify
        }
    }
}