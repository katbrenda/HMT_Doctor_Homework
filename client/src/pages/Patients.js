import React from "react"
//import useAxiosOnMount from "../src/hooks/useAxiosOnMount";

const Patients = ()=>{
    return (
        <div>
            <h1>Patients</h1>
        </div>

    )
}

export default Patients





//     const {response, loading, error} = useAxiosOnMount('/api/physicions')

//      if (loading) return <p>loading</p>
//      if(error) return <p>error</p>

